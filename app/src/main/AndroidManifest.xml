<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pos.minishop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniShop">
        <activity android:name=".ui.CreateMemberActivity" />
        <activity android:name=".ui.EditMemberActivity" />
        <activity android:name=".ui.DetailTransActivity" />
        <activity android:name=".ui.HistoryTransActivity" />
        <activity android:name=".ui.ListMemberActivity" />
        <activity android:name=".ui.EditDiscountActivity" />
        <activity android:name=".ui.CreateDiscountActivity" />
        <activity android:name=".ui.EditProductActivity" />
        <activity android:name=".ui.EditCategoryActivity" />
        <activity android:name=".ui.ChargeActivity" />
        <activity android:name=".ui.CartActivity" />
        <activity android:name=".ui.DiscountManagementActivity" />
        <activity android:name=".ui.ResultActivity" />
        <activity android:name=".ui.CalcuActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.CategoryManagementActivity" />
        <activity android:name=".ui.InputProductActivity" />
        <activity android:name=".ui.ProductManagementActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MiniShop.NoActionBar" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>